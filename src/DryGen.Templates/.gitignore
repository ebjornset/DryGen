# Ignore the test folder
test
# Ignore the generated folders, since they will be recreated when the user installs and builds the templates
templates/**/generated
# Ignore the generated files, since they will be recreated when the user installs and builds the templates
templates/**/*.generated.*
# Ignore the local tool-manifest folder as this is regenerated with the correct dry-gen version during packaging
templates/**/.config