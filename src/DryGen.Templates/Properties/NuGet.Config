<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <!-- Make sure we have no other package sources than the local artifacts when generating dotnet-tools.json from Nuke -->
  </packageSources>
</configuration>
